# 广告营销任务路由规则（精简版）

## 说明

为避免 AGENTS.md 臃肿，本文档独立记录广告 skills 的触发规则。各 workspace 的 AGENTS.md 只需在 `### 0.1 先查 Skill` 章节末尾添加一行引用即可：

```markdown
- **广告营销任务**：参考 `广告营销任务路由规则.md`
```

---

## main agent 触发规则

| 用户输入 | 调用 Skill | 说明 |
|---------|-----------|------|
| 品牌全案、整合营销、营销战役、Launch Campaign | `boss` | 统筹完整流程，自动协调各岗位 |
| Brief收集、客户需求、需求整理、会议纪要、反馈整理、项目协调 | `kefu-ae` | 整理需求，列出资料缺口 |

**工作流**：
- `kefu-ae` 整理完 Brief → 派发 `strategy` agent
- `strategy` 产出策略 → 回传 `main` 确认 → 派发 `copywriter`/`design`
- `copywriter`/`design` 产出执行物料 → 回传 `main` 统一交付

---

## strategy agent 触发规则

| 用户输入 | 调用 Skill | 说明 |
|---------|-----------|------|
| 定位、品牌策略、受众洞察、Creative Brief | `celue-zj` | 策略制定，必须在问题和目标三方确认后执行 |
| 竞品分析 | `celue-zj` + `multi-search-engine` | 先规划分析框架，再调用搜索引擎，最后输出分析报告 |
| 创意方向、Big Idea、概念选择、创意评审 | `chuangyi-zj` | 基于已确认策略制定创意方向 |

**前置条件**：
- 策略任务必须在 AE、策略、创意三方确认问题和目标后才能执行
- 缺少行业调研、竞品资料、消费者资料时，先列出缺口，不得编造假设

---

## copywriter agent 触发规则

| 用户输入 | 调用 Skill | 说明 |
|---------|-----------|------|
| Campaign主题、品牌Slogan、KV文案、社媒文案、视频脚本、文案打磨 | `wenan` | 必须在创意方向三方确认后执行 |

**前置条件**：
- 必须有已确认的创意方向、策略 brief、目标人群、品牌语气
- 缺少关键信息时，先列出缺口

**输出规范**：
- 默认提供 2-3 个版本，每版本说明适用场景和取舍理由
- 文案写入 `outputs/copy_YYYYMMDD_主题.md`

**边界说明**：
- `brand-poster-creator` 的海报文案是执行层，不触发 `wenan` skill
- `wenan` skill 负责战略层文案（Campaign 主题、Slogan、品牌主张）

---

## design agent 触发规则

| 用户输入 | 调用 Skill | 说明 |
|---------|-----------|------|
| 视觉方向、KV概念、品牌视觉系统、moodboard、设计方向 | `sheji` | 输出视觉方向和 AI prompt，不直接生图 |

**前置条件**：
- 必须有已确认的创意方向、策略主张、文案内容、品牌规范
- 缺少品牌资产、尺寸规范、材质要求时，先列出缺口

**工作流**：
```
用户需求 → sheji skill 输出视觉方向 → 用户确认 → 调用执行 skills
                                              ↓
                                        brand-poster-creator
                                        product-photography-workflow
                                        xiangqingye-desigen
                                        gpt-image2-gen
```

**边界说明**：
- `sheji` skill 是方法论层（输出视觉方向）
- 现有 design skills 是执行工具层（直接生图）
- 两者协作关系，不冲突

**跳过规则**：
- 如果用户明确说"品牌海报"、"详情页"、"产品摄影"等具体执行物料，直接调用对应执行 skill，不需要先调用 `sheji` skill

---

## 触发优先级

当任务匹配多个 skill 时，按以下优先级：

1. **强制路由规则**（AGENTS.md 中明确定义的 `###` 章节）优先级最高
2. **广告 skills** 次之（方法论层）
3. **执行 skills** 最后（工具层）

**示例**：
- 用户说"品牌海报" → 触发 `brand-poster-creator`（强制路由，AGENTS.md 已定义）
- 用户说"视觉方向" → 触发 `sheji` skill（广告 skill）
- 用户说"生成一张图" → 触发 `gpt-image2-gen`（执行 skill）
