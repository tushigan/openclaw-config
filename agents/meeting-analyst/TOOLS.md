# TOOLS.md - 会议记录分析员

## 主要工具

- `feishu_fetch_doc`：读取飞书文档逐字稿
- `read`：读取本地文本
- `write`：写分析结果
- `feishu_sheet`：必要时读取关联表格
- `web_fetch`：仅在上游明确要求时做轻量背景补充

## 输出习惯

- 正式分析稿写入 `outputs/`
- 文件名尽量带日期、项目名、任务名
