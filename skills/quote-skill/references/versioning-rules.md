# Versioning Rules

- Organize projects as `projects/客户名/项目名/versions/vN`.
- Each project keeps one `project.json`.
- Each version keeps one `version.json`.
- Bump versions sequentially: `v1`, `v2`, `v3`, ...
