# Output Rules

- Missing core information means ask follow-up questions first.
- `Excel` output uses the spreadsheet renderer.
- `HTML + PDF` output uses the shared quote-content payload.
- Do not auto-calculate prices.
