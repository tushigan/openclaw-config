# OpenClaw 配置项目全量检查报告

**检查时间**: 2026-06-13  
**检查范围**: ~/.openclaw 配置目录  
**检查方式**: 配置文件分析、文件结构审计、一致性验证

---

## 一、发现的问题 (Bugs)

### 1. ⚠️ 高优先级问题

#### 1.1 工具引用不一致：`feishu-send-image` 工具不存在
**位置**: `openclaw.json`  
**影响范围**: 多个 agent 的 `tools.alsoAllow` 配置  
**具体行数**: 313, 406, 515, 602, 712 等共 13 处

**问题描述**:  
配置中多处引用了 `feishu-send-image` 工具，但根据 CLAUDE.md 和 memory 记录，该工具已被废弃。正确的做法是使用 `message` 工具的 `path` 参数发送图片。

**影响**:
- Agent 配置中存在无效工具引用
- 可能导致混淆，让 agent 尝试调用不存在的工具
- 与文档规范（CLAUDE.md）不一致

**建议修复**:
```bash
# 从 openclaw.json 中移除所有 "feishu-send-image" 引用
# 受影响的 agent: main, strategy, design, research, copywriter, main-shared, 
# strategy-shared, design-shared, research-shared, copywriter-shared, business-shared
```

#### 1.2 历史损坏文件未清理
**位置**: 根目录  
**文件数量**: 7 个 `.broken` 和 `.clobbered` 文件

**文件列表**:
- `openclaw.json.truncated-before-restore.1775990085`
- `openclaw.json.broken`
- `openclaw.json.clobbered.*` (5个时间戳版本)
- `cron/jobs.json.broken`

**问题描述**:  
这些是历史故障恢复过程中留下的备份文件，占用空间且容易造成混淆。

**建议修复**:
```bash
# 清理历史损坏文件
rm ~/.openclaw/openclaw.json.broken
rm ~/.openclaw/openclaw.json.truncated-*
rm ~/.openclaw/openclaw.json.clobbered.*
rm ~/.openclaw/cron/jobs.json.broken
```

#### 1.3 `cron/jobs.json` 文件缺失
**位置**: `/Users/a123/.openclaw/cron/jobs.json`  
**问题描述**: 文件不存在，但 CLAUDE.md 中描述该文件应包含定时任务配置

**影响**:
- 定时任务功能不可用
- Memory Dreaming Promotion (每日 3am)
- AI圈24h晨报 (每日 7am)

**建议修复**:
需要确认是否需要这些定时任务，如果需要则创建配置文件。

---

### 2. ⚡ 中优先级问题

#### 2.1 CLAUDE.md 与 AGENTS.md 信息不同步

**位置**: `/Users/a123/.openclaw/CLAUDE.md` vs 实际配置

**不一致点**:

1. **模型 provider 名称过时**:
   - CLAUDE.md 提到 `zhichuang`, `huoshan`, `newapi_channel_conn`
   - 实际 openclaw.json 使用 `aixor`, `cc-vibe`, `aixor-g`

2. **Agent 主模型描述不准确**:
   - CLAUDE.md 说 main 用 GPT-5.4 (fallback Kimi K2.6)
   - 实际 openclaw.json 中 main 用 `cc-vibe/claude-opus-4-8`
   
   - CLAUDE.md 说 strategy 用 GPT-5.5
   - 实际 openclaw.json 中 strategy 用 `cc-vibe/claude-opus-4-8`

3. **Agent 表格信息过时**:
   - CLAUDE.md 中 design/research/meeting-analyst 说用 Kimi K2.5
   - 实际配置分别为 GPT-5.5, GPT-5.4, GPT-5.4

**建议修复**:  
更新 CLAUDE.md 的 "Model Providers" 和 "Agent Architecture" 章节，与 openclaw.json 实际配置对齐。

#### 2.2 根目录 AGENTS.md 中提到的 Codex
**位置**: `/Users/a123/.openclaw/AGENTS.md` 第 3、11、12 行

**问题描述**:  
文件开头提到 "Codex" 和 "Codex.ai/code"，但这应该是 "Claude Code"。可能是早期模板遗留。

**建议修复**:
```bash
# 将 AGENTS.md 中的 Codex 替换为 Claude Code
sed -i.bak 's/Codex/Claude Code/g' ~/.openclaw/AGENTS.md
```

#### 2.3 会话文件数量巨大：main-shared 有 1843 个会话
**位置**: `/Users/a123/.openclaw/agents/main-shared/sessions/`

**问题描述**:  
main-shared 累积了 1843 个会话文件，可能影响性能和磁盘占用。

**当前状态**:
- main-shared: 1843 个会话文件
- design-shared: 833 个
- research-shared: 364 个
- research: 203 个

**建议**:  
根据 `openclaw.json` 中的 `session.maintenance` 配置，应该已经设置了清理策略（pruneAfter: 7天，maxEntries: 300）。建议：
1. 验证自动清理是否正常运行
2. 可以运行手动清理脚本 `scripts/safe-cleanup-sessions.sh`

---

### 3. 🔧 低优先级问题

#### 3.1 日志文件较大
**位置**: `/Users/a123/.openclaw/logs/gateway.log`  
**大小**: 22MB

**建议**: 定期归档或清理日志，或配置 log rotation。

#### 3.2 agents 目录占用 4GB 空间
**位置**: `/Users/a123/.openclaw/agents/`

**组成**:
- 会话文件 (sessions/)
- 可能的缓存数据

**建议**: 定期清理旧会话，或验证 session maintenance 策略是否生效。

#### 3.3 Doctor 警告信息
**内容**: Memory Core 短期记忆和 phase signals 导入被跳过（因为 SQLite 已有数据）

**状态**: 这是正常的迁移提示，不是错误，可忽略。

#### 3.4 全局 skills 数量较少
**当前**: 仅 10 个全局 skill 有 SKILL.md

**说明**: 大部分 skills 是软链接到 lark-* 系列技能。这是正常架构设计，但要注意：
- `anysearch` 和 `boss` 等关键 skill 是否有完整文档
- 各 workspace 的专用 skills 是否都有文档

---

## 二、优化建议 (Improvements)

### 1. 📝 文档与配置同步

#### 1.1 建立配置文档同步检查机制
**目标**: 避免 CLAUDE.md 与 openclaw.json 长期不同步

**方案**:
1. 编写脚本 `scripts/validate-docs-sync.py`，对比关键配置点
2. 在更新 openclaw.json 后运行验证
3. 将验证加入 git pre-commit hook

#### 1.2 添加配置版本号和更新日期
**当前**: `openclaw.json` 有 `meta.lastTouchedVersion` 和 `meta.lastTouchedAt`

**建议**: 在 CLAUDE.md 开头也添加：
```markdown
# CLAUDE.md
**配置版本**: 2026.6.6  
**最后更新**: 2026-06-11  
**同步状态**: ✅ 已与 openclaw.json 同步
```

---

### 2. 🧹 清理与维护自动化

#### 2.1 定期清理脚本调度
**当前状态**: scripts/ 目录已有清理脚本，但未集成到 cron

**建议**:
1. 恢复 `cron/jobs.json` 配置
2. 添加清理任务：
   - 每周清理旧会话 (`cleanup-weekly-medium.sh`)
   - 每日轻量清理 (`cleanup-daily-light.sh`)
   - 每月深度清理 (`cleanup-monthly-deep.sh`)

#### 2.2 日志轮转配置
**方案**: 使用 logrotate 或内置日志管理
```bash
# 示例 logrotate 配置
/Users/a123/.openclaw/logs/*.log {
    weekly
    rotate 4
    compress
    missingok
    notifempty
}
```

---

### 3. 🔒 安全性增强

#### 3.1 敏感文件保护检查
**当前**: .gitignore 已经配置了敏感文件排除

**建议**: 定期审计，确保没有 API key 或 token 泄露到 git：
```bash
# 检查脚本
git log --all --full-history --source --find-copies-harder -- "*.json" | \
grep -i "api.*key\|token\|secret"
```

#### 3.2 exec-approvals.json 权限审计
**当前**: 配置了详细的命令白名单

**建议**: 
1. 定期审查 `allow-always` 来源的条目
2. 清理长期未使用的批准（如 lastUsedAt 超过 3 个月）

---

### 4. 🎯 性能优化

#### 4.1 Session 清理策略优化
**当前配置**:
```json
"session": {
  "maintenance": {
    "mode": "enforce",
    "pruneAfter": "7d",
    "maxEntries": 300
  }
}
```

**问题**: main-shared 仍有 1843 个会话，远超 maxEntries

**建议**:
1. 检查 maintenance 是否真正执行
2. 考虑缩短 pruneAfter 到 3d
3. 降低 maxEntries 到 100

#### 4.2 内存数据库优化
**位置**: `memory/*.sqlite`  
**大小**: 60MB

**建议**:
1. 定期 VACUUM 压缩数据库
2. 清理过期的短期记忆
3. 验证索引是否优化

---

### 5. 📊 监控与可观测性

#### 5.1 添加健康检查定时任务
**方案**: 每日运行 `openclaw doctor` 并记录结果
```bash
# cron 任务示例
0 2 * * * cd ~/.openclaw && openclaw doctor > logs/health-check-$(date +\%Y\%m\%d).log 2>&1
```

#### 5.2 关键指标监控脚本
**建议创建**: `scripts/system-metrics.sh`

**监控项**:
- 各 agent 会话数量
- 日志文件大小
- 内存数据库大小
- 损坏文件检测
- 配置文件语法检查

---

### 6. 🏗️ 架构改进

#### 6.1 Workspace 规则文档标准化
**当前**: 各 workspace 的 AGENTS.md 长度和结构差异较大
- workspace/AGENTS.md: 426 行
- workspace-design/AGENTS.md: 419 行
- workspace-meeting/AGENTS.md: 111 行

**建议**: 制定 AGENTS.md 模板规范，确保关键章节一致。

#### 6.2 Skills 文档完整性检查
**当前**: 只检查到 10 个全局 skill 有 SKILL.md

**建议**:
```bash
# 创建验证脚本
scripts/validate-skills-docs.sh
# 检查所有 skill 目录是否有 SKILL.md
# 检查 SKILL.md 是否包含必要章节
```

---

## 三、检查总结

### ✅ 运行正常的部分
1. **配置引用一致性**: 所有 agent、model、provider 引用都有效
2. **Git 追踪策略**: .gitignore 配置合理，敏感文件已排除
3. **工具权限管理**: exec-approvals.json 配置详细且活跃
4. **MCP 集成**: Apifox MCP server 配置正确
5. **多 agent 架构**: 14 个 agent 配置完整，角色分工清晰
6. **核心功能**: gateway、channels、bindings 配置正确

### ⚠️ 需要修复的问题
1. **高优先级** (3个): feishu-send-image 引用、损坏文件、cron 配置缺失
2. **中优先级** (3个): 文档不同步、会话堆积、Codex 命名错误
3. **低优先级** (4个): 日志大小、磁盘占用、doctor 警告

### 🎯 建议优化方向
1. **文档同步**: 自动化检查配置与文档一致性
2. **清理自动化**: 恢复定时清理任务
3. **性能优化**: session maintenance 真正生效
4. **监控增强**: 添加健康检查和指标监控
5. **标准化**: skill 和 workspace 文档规范

---

## 四、下一步行动建议

### 立即执行 (今天)
1. ✅ 从 openclaw.json 移除 `feishu-send-image` 引用
2. ✅ 清理历史损坏文件
3. ✅ 更新 CLAUDE.md 的 provider 和 model 信息

### 本周执行
4. 检查并恢复 cron/jobs.json 配置
5. 验证 session maintenance 是否生效
6. 运行 `safe-cleanup-sessions.sh` 清理旧会话

### 本月执行
7. 编写配置文档同步验证脚本
8. 添加定时健康检查
9. 制定 workspace AGENTS.md 模板规范
10. 清理 3 个月未使用的 exec-approvals 条目

---

## 附录：检查方法说明

本次检查使用的方法：
1. **配置文件分析**: 解析 openclaw.json 语法和引用关系
2. **文档对比**: CLAUDE.md vs openclaw.json
3. **文件结构审计**: 检查目录、文件数量、大小
4. **一致性验证**: agent/model/provider/tool 引用完整性
5. **健康检查**: 运行 `openclaw doctor`
6. **Git 状态**: 查看未跟踪和修改的文件

**检查覆盖率**:
- ✅ 核心配置文件 (openclaw.json, exec-approvals.json, .gitignore, .mcp.json)
- ✅ Workspace 规则文件 (7个 workspace 的 AGENTS.md)
- ✅ 根目录文档 (CLAUDE.md, AGENTS.md)
- ✅ Skills 目录结构
- ✅ 运行时数据 (logs, memory, sessions, agents)
- ⚠️ 未深入检查：各 workspace 的 SOUL.md, IDENTITY.md, USER.md

---

**报告生成时间**: 2026-06-13 12:36  
**检查执行者**: Claude Code (Opus 4.8)
